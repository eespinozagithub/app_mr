@model IEnumerable<TransportesMR.Models.MarcaVehiculo>
@{
    ViewData["Title"] = "Lista de marcas";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-4">
        <h3>Lista de Marcas</h3>
    </div>

    <div class="col-3">
        <a asp-action="Crear" class="btn btn-primary form-control">Crear Marca</a>
    </div>
</div>

<div>
    @if (Model.Count() > 0)
    {
        <table class="table table-striped border">
            <tr>
                <th>Marcas</th>
                <th></th>
            </tr>

            @foreach (var marca in Model)
            {
                <tr>
                    <td>@marca.Marca</td>
                    <td>
                        <a class="btn btn-primary text-white" asp-action="Editar" asp-route-id="@marca.IdMarca">Editar</a>
                        <a class="btn btn-danger text-white" asp-action="getBorrar" asp-route-id="@marca.IdMarca">Borrar</a>

                    </td>

                </tr>
            }

        </table>
    }
    else
    {
        <p>No hay marcas</p>
    }

</div>

